{
  "name": "instagram",
  "version": "1.0.0",
  "description": "crawl instagram",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:tailwind": "npx tailwindcss -i ./src/styles/tailwind.css -o ./src/styles/output.css --watch",
    "start": "electron .",
    "build": "electron-builder",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "puppeteer": "^23.6.1"
  }
}
